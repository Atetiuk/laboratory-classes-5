<ul>
  <% if (products.length > 0) { %>
    <% products.forEach(product => { %>
      <li><%- include('partials/card', { product }) %></li>
    <% }) %>
  <% } else { %>
    <li>No products available</li>
  <% } %>
</ul>

